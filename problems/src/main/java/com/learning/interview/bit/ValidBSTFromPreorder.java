package com.learning.interview.bit;

import java.util.ArrayList;

public class ValidBSTFromPreorder {

    public int solve(ArrayList<Integer> A) {
        if(A == null || A.size() < 2) return 1;
        int rootNode = A.get(0);
        return 1;
    }

}
